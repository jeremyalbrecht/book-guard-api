meta {
  name: Get Book
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/books/{{bookId}}
  body: none
  auth: inherit
}

tests {
  test("returns the created book", function () {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().id).to.equal(bru.getEnvVar("bookId"));
  });
}
