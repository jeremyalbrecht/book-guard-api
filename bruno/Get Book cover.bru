meta {
  name: Get Book cover
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/book/{{bookId}}/cover
  body: none
  auth: inherit
}

tests {
  test("returns the caller's books", function () {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody()).to.be.an("array");
  });
}
